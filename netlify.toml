[build]
  command = ""
  publish = "."
  functions = "netlify/functions"

[dev]
  functions = "netlify/functions"

# Redirect API requests to the functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
